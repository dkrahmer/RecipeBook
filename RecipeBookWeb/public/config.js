// eslint-disable-next-line
var _config = {
	appName: "Recipe Book",				// The public title of the app
	authTokenKey: "auth_token",			// Cookie name to use to store the auth token

	debugMode: true,					// Allows full access without logging in. Set to false for production.
	apiUrl: "http://localhost:51301",	// URL for the web API
	googleClientId: "<your-id-here>.apps.googleusercontent.com"

	// Generate Google client ID: https://developers.google.com/identity/sign-in/web/sign-in
};
